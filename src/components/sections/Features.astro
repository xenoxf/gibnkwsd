<section class="features">
  <div class="section-header">
    <span class="section-label">// Por qué OpenJunior</span>
    <h2 class="section-title" style="font-size: clamp(2rem, 4vw, 3rem);">Hecho para <span class="gradient-text">todos</span></h2>
  </div>

  <div class="features-grid">
    <div class="feature-card reveal reveal-delay-1">
      <div class="feature-card-icon">
        <svg width="20" height="20" viewBox="0 0 20 20" fill="none">
          <rect x="2" y="3" width="16" height="12" rx="2" stroke="currentColor" stroke-width="1.5"/>
          <path d="M6 19h8M10 15v4" stroke="currentColor" stroke-width="1.5"/>
        </svg>
      </div>
      <h3>Catálogo MCP</h3>
      <p>Navega e instala herramientas de IA con un clic. Como los conectores de Claude, pero en un catálogo visual con búsqueda y filtros.</p>
    </div>

    <div class="feature-card reveal reveal-delay-2">
      <div class="feature-card-icon">
        <svg width="20" height="20" viewBox="0 0 20 20" fill="none">
          <rect x="2" y="3" width="16" height="12" rx="2" stroke="currentColor" stroke-width="1.5"/>
          <path d="M6 19h8M10 15v4" stroke="currentColor" stroke-width="1.5"/>
        </svg>
      </div>
      <h3>1000+ Integraciones</h3>
      <p>Conecta Gmail, Slack, Google Drive, Notion y más vía Composio. Tu asistente de IA actúa sobre tus servicios favoritos.</p>
    </div>

    <div class="feature-card reveal reveal-delay-3">
      <div class="feature-card-icon">
        <svg width="20" height="20" viewBox="0 0 20 20" fill="none">
          <path d="M12 2l4 4-4 4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
          <path d="M2 10h10" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
          <path d="M2 14h14" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
          <path d="M2 18h12" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
        </svg>
      </div>
      <h3>Skills Reutilizables</h3>
      <p>Crea, instala y comparte automatizaciones. Skills reutilizables entre proyectos con un catálogo integrado.</p>
    </div>
  </div>
</section>

<style is:global>
.features {
  position: relative;
  z-index: 1;
  padding: 5rem 2rem;
}

.features-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 1.5rem;
}

.feature-card {
  background: rgba(255, 255, 255, 0.03);
  backdrop-filter: blur(16px);
  -webkit-backdrop-filter: blur(16px);
  border: 1px solid rgba(255, 255, 255, 0.1);
  border-radius: var(--radius-sm);
  padding: 2rem;
  transition: border-color 0.4s var(--ease-out), transform 0.4s var(--ease-out);
}

.feature-card:hover {
  border-color: rgba(204, 255, 0, 0.4);
  transform: translateY(-2px);
}

.feature-card-icon {
  width: 40px;
  height: 40px;
  display: flex;
  align-items: center;
  justify-content: center;
  background: rgba(204, 255, 0, 0.1);
  border-radius: 0.75rem;
  color: var(--accent);
  margin-bottom: 1.25rem;
}

.feature-card h3 {
  font-family: var(--font-heading);
  font-weight: 600;
  font-size: 1.05rem;
  letter-spacing: -0.03em;
  margin-bottom: 0.5rem;
}

.feature-card p {
  font-size: 0.85rem;
  color: var(--text-secondary);
  line-height: 1.7;
}
</style>