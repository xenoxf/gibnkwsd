<section class="features">
  <div class="section-header">
    <span class="section-label">// Why OpenJunior</span>
    <h2 class="section-title" style="font-size: clamp(2rem, 4vw, 3rem);">Built for <span class="gradient-text">developers</span></h2>
  </div>

  <div class="features-grid">
    <div class="feature-card reveal reveal-delay-1">
      <div class="feature-card-icon">
        <svg width="20" height="20" viewBox="0 0 20 20" fill="none">
          <path d="M10 20s6-3 6-8V4l-6-2-6 2v8c0 5 6 8 6 8z" stroke="currentColor" stroke-width="1.5"/>
        </svg>
      </div>
      <h3>Privacy First</h3>
      <p>Runs on your machine. Your code never leaves your computer. No telemetry, no tracking.</p>
    </div>

    <div class="feature-card reveal reveal-delay-2">
      <div class="feature-card-icon">
        <svg width="20" height="20" viewBox="0 0 20 20" fill="none">
          <path d="M2 10h16M10 2v16" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
        </svg>
      </div>
      <h3>Cross-Device Continuity</h3>
      <p>Start in TUI, continue on tablet/phone, return to terminal — same session, same context.</p>
    </div>

    <div class="feature-card reveal reveal-delay-3">
      <div class="feature-card-icon">
        <svg width="20" height="20" viewBox="0 0 20 20" fill="none">
          <circle cx="10" cy="10" r="8" stroke="currentColor" stroke-width="1.5"/>
          <path d="M10 6v4l2.5 2.5" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
        </svg>
      </div>
      <h3>Real-Time Collaboration</h3>
      <p>Multi-agent runs, inline comments on diffs, and shareable session links with tunnel access.</p>
    </div>
  </div>
</section>

<style is:global>
.features {
  position: relative;
  z-index: 1;
  padding: 5rem 2rem;
}

.features-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 1.5rem;
}

.feature-card {
  background: rgba(255, 255, 255, 0.03);
  backdrop-filter: blur(16px);
  -webkit-backdrop-filter: blur(16px);
  border: 1px solid rgba(255, 255, 255, 0.1);
  border-radius: var(--radius-sm);
  padding: 2rem;
  transition: border-color 0.4s var(--ease-out), transform 0.4s var(--ease-out);
}

.feature-card:hover {
  border-color: rgba(204, 255, 0, 0.4);
  transform: translateY(-2px);
}

.feature-card-icon {
  width: 40px;
  height: 40px;
  display: flex;
  align-items: center;
  justify-content: center;
  background: rgba(204, 255, 0, 0.1);
  border-radius: 0.75rem;
  color: var(--accent);
  margin-bottom: 1.25rem;
}

.feature-card h3 {
  font-family: var(--font-heading);
  font-weight: 600;
  font-size: 1.05rem;
  letter-spacing: -0.03em;
  margin-bottom: 0.5rem;
}

.feature-card p {
  font-size: 0.85rem;
  color: var(--text-secondary);
  line-height: 1.7;
}
</style>
