<section id="features" class="bento">
  <div class="section-header">
    <span class="section-label">// Capacidades</span>
    <h2 class="section-title" style="font-size: clamp(2rem, 4vw, 3rem);">Todo lo que necesitas con <span class="gradient-text">IA</span></h2>
    <p class="section-desc" style="margin-top: 0.75rem;">Catálogo MCP, integraciones con servicios, skills reutilizables y más — todo en un solo lugar.</p>
  </div>

  <div class="bento-grid">
    <div class="bento-card bento-card--large reveal reveal-delay-1">
      <span class="bento-card-label">Chat</span>
      <h3>Chat Ramificable</h3>
      <p class="bento-card-desc">Deshaz, rehaz y bifurca desde cualquier turno. Explora diferentes enfoques sin perder tu lugar.</p>
      <div class="bento-chart">
        <div class="bento-bar" style="height: 40%;"></div>
        <div class="bento-bar" style="height: 65%;"></div>
        <div class="bento-bar" style="height: 30%;"></div>
        <div class="bento-bar" style="height: 85%;"></div>
        <div class="bento-bar" style="height: 55%;"></div>
        <div class="bento-bar" style="height: 90%;"></div>
        <div class="bento-bar" style="height: 45%;"></div>
        <div class="bento-bar" style="height: 75%;"></div>
      </div>
    </div>

    <div class="bento-card reveal reveal-delay-2">
      <div class="bento-card-icon">
        <svg width="18" height="18" viewBox="0 0 20 20" fill="none">
          <path d="M4 4h12v12H4V4z" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
          <path d="M7 10l2 2 4-4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
        </svg>
      </div>
      <h3>Git &amp; GitHub</h3>
      <p class="bento-card-desc">Git visual con staging, commits, creación de PRs, status checks y merge actions. Sin terminal.</p>
    </div>

    <div class="bento-card bento-card--tall reveal reveal-delay-3">
      <span class="bento-card-label">MCP</span>
      <h3>Catálogo de MCPs</h3>
      <p class="bento-card-desc">Navega, instala y configura servidores MCP con un clic. Como los conectores de Claude, pero mejor.</p>
      <div class="bento-swatches">
        <span class="bento-swatch" style="background: #ccff00;"></span>
        <span class="bento-swatch" style="background: #10b981;"></span>
        <span class="bento-swatch" style="background: #6366f1;"></span>
        <span class="bento-swatch" style="background: #f59e0b;"></span>
        <span class="bento-swatch" style="background: #ef4444;"></span>
        <span class="bento-swatch" style="background: #ec4899;"></span>
        <span class="bento-swatch" style="background: #0c0c0c;"></span>
        <span class="bento-swatch" style="background: #ebebeb;"></span>
      </div>
    </div>

    <div class="bento-card reveal reveal-delay-2">
      <div class="bento-card-icon">
        <svg width="18" height="18" viewBox="0 0 20 20" fill="none">
          <rect x="2" y="3" width="16" height="12" rx="2" stroke="currentColor" stroke-width="1.5"/>
          <path d="M6 19h8M10 15v4" stroke="currentColor" stroke-width="1.5"/>
        </svg>
      </div>
      <h3>Desktop + Web + Móvil</h3>
      <p class="bento-card-desc">Usa como app de escritorio, en el navegador, PWA o VS Code. La misma sesión en todas partes.</p>
    </div>

    <div class="bento-card reveal reveal-delay-3">
      <div class="bento-card-icon">
        <svg width="18" height="18" viewBox="0 0 20 20" fill="none">
          <path d="M4 14l6-6-6-6M12 16h6" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
        </svg>
      </div>
      <h3>Integraciones</h3>
      <p class="bento-card-desc">Conecta Gmail, Slack, Google Drive, Notion y 1000+ servicios más vía Composio con OAuth.</p>
    </div>

    <div class="bento-card bento-card--accent reveal reveal-delay-4">
      <div class="bento-noise-overlay"></div>
      <span class="bento-card-label">Skills</span>
      <h3>Skills Reutilizables</h3>
      <p class="bento-card-desc">Crea, instala y comparte automatizaciones reutilizables. Catálogo integrado con ClawdHub.</p>
      <div class="bento-card-icon" style="margin-top: auto;">
        <svg width="18" height="18" viewBox="0 0 20 20" fill="none">
          <rect x="7" y="2" width="6" height="10" rx="3" stroke="currentColor" stroke-width="1.5"/>
          <path d="M4 10a6 6 0 0012 0" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
          <path d="M10 16v3" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
        </svg>
      </div>
    </div>

    <div class="bento-card reveal reveal-delay-3">
      <div class="bento-card-icon">
        <svg width="18" height="18" viewBox="0 0 20 20" fill="none">
          <circle cx="10" cy="10" r="8" stroke="currentColor" stroke-width="1.5"/>
          <path d="M10 6v4l3 3" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
        </svg>
      </div>
      <h3>Multi-dispositivo</h3>
      <p class="bento-card-desc">Empieza en desktop, continúa en tablet, vuelve al terminal. Túneles Cloudflare con QR.</p>
    </div>

    <div class="bento-card reveal reveal-delay-4">
      <div class="bento-card-icon">
        <svg width="18" height="18" viewBox="0 0 20 20" fill="none">
          <path d="M12 2l4 4-4 4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
          <path d="M2 10h10" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
          <path d="M2 14h14" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
          <path d="M2 18h12" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
        </svg>
      </div>
      <h3>Modo Voz</h3>
      <p class="bento-card-desc">Entrada por voz y respuestas en voz alta para flujos de trabajo manos libres.</p>
    </div>
  </div>
</section>

<style is:global>
.bento {
  position: relative;
  z-index: 1;
  padding: 5rem 2rem;
}

.bento-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  grid-auto-rows: 1fr;
  gap: 1rem;
}

.bento-card {
  background: rgba(255, 255, 255, 0.03);
  backdrop-filter: blur(16px);
  -webkit-backdrop-filter: blur(16px);
  border: 1px solid rgba(255, 255, 255, 0.1);
  border-radius: var(--radius-lg);
  padding: 2rem;
  transition: border-color 0.4s var(--ease-out), transform 0.4s var(--ease-out), box-shadow 0.4s var(--ease-out);
  display: flex;
  flex-direction: column;
  position: relative;
  overflow: hidden;
}

.bento-card:hover {
  border-color: rgba(204, 255, 0, 0.4);
  transform: translateY(-2px);
  box-shadow: 0 8px 40px rgba(0, 0, 0, 0.3);
}

.bento-card--large {
  grid-column: span 2;
  grid-row: span 2;
}

.bento-card--tall {
  grid-column: span 1;
  grid-row: span 2;
}

.bento-card--accent {
  background: var(--accent);
  color: #000000;
  border-color: var(--accent);
}

.bento-card--accent:hover {
  border-color: var(--accent);
}

.bento-card--accent .bento-card-icon {
  background: rgba(0, 0, 0, 0.1);
}

.bento-card--accent .bento-card-desc {
  color: rgba(0, 0, 0, 0.7);
}

.bento-card-label {
  font-family: var(--font-mono);
  font-size: 0.6rem;
  text-transform: uppercase;
  letter-spacing: 0.15em;
  color: var(--accent);
  margin-bottom: 0.5rem;
}

.bento-card--accent .bento-card-label {
  color: rgba(0, 0, 0, 0.5);
}

.bento-card-icon {
  width: 36px;
  height: 36px;
  display: flex;
  align-items: center;
  justify-content: center;
  background: rgba(204, 255, 0, 0.1);
  border-radius: 0.625rem;
  color: var(--accent);
  margin-bottom: 1rem;
  flex-shrink: 0;
}

.bento-card--accent .bento-card-icon {
  color: #000000;
}

.bento-card h3 {
  font-family: var(--font-heading);
  font-weight: 600;
  font-size: 1.1rem;
  letter-spacing: -0.03em;
  margin-bottom: 0.5rem;
  line-height: 1.3;
}

.bento-card-desc {
  font-size: 0.85rem;
  color: var(--text-secondary);
  line-height: 1.6;
  flex: 1;
}

.bento-chart {
  display: flex;
  align-items: flex-end;
  gap: 0.5rem;
  margin-top: auto;
  padding-top: 1.5rem;
  height: 120px;
}

.bento-bar {
  flex: 1;
  border-radius: 0.375rem 0.375rem 0 0;
  background: linear-gradient(to top, rgba(204, 255, 0, 0.15), rgba(204, 255, 0, 0.4));
  min-height: 20px;
  transition: min-height 0.5s var(--ease-out);
}

.bento-card:hover .bento-bar {
  background: linear-gradient(to top, rgba(204, 255, 0, 0.2), rgba(204, 255, 0, 0.6));
}

.bento-swatches {
  display: flex;
  flex-wrap: wrap;
  gap: 0.5rem;
  margin-top: auto;
  padding-top: 1.5rem;
}

.bento-swatch {
  width: 36px;
  height: 36px;
  border-radius: 0.5rem;
  border: 1px solid rgba(255, 255, 255, 0.1);
}

.bento-noise-overlay {
  position: absolute;
  inset: 0;
  pointer-events: none;
  opacity: 0.08;
  mix-blend-mode: overlay;
  background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)'/%3E%3C/svg%3E");
  background-size: 128px 128px;
}
</style>
