<section id="features" class="bento">
  <div class="section-header section-header--center">
    <span class="section-label">// Qué sabe hacer</span>
    <h2 class="section-title" style="font-size: clamp(2rem, 4vw, 3rem);">Todo lo que necesitas, <span class="gradient-text">en un solo lugar</span></h2>
    <p class="section-desc">No son promesas de marketing: son cosas que usas todos los días y que por fin funcionan solas.</p>
  </div>

  <div class="bento-grid">
    <div class="bento-card bento-card--large glass-card reveal reveal-delay-1">
      <span class="bento-card-label">Chat</span>
      <h3>Una charla que no se pierde</h3>
      <p class="bento-card-desc">Si una respuesta no te convence, retrocedes, pruebas otro camino y comparas sin volver a explicarle nada. Como tener borrador y versión final al mismo tiempo.</p>
      <div class="bento-chart" aria-hidden="true">
        <div class="bento-bar" style="height: 40%;"></div>
        <div class="bento-bar" style="height: 65%;"></div>
        <div class="bento-bar" style="height: 30%;"></div>
        <div class="bento-bar" style="height: 85%;"></div>
        <div class="bento-bar" style="height: 55%;"></div>
        <div class="bento-bar" style="height: 90%;"></div>
        <div class="bento-bar" style="height: 45%;"></div>
        <div class="bento-bar" style="height: 75%;"></div>
      </div>
    </div>

    <div class="bento-card glass-card reveal reveal-delay-2">
      <div class="bento-card-icon" aria-hidden="true">
        <svg width="18" height="18" viewBox="0 0 20 20" fill="none">
          <path d="M4 4h12v12H4V4z" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
          <path d="M7 10l2 2 4-4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
        </svg>
      </div>
      <h3>Git sin tocar la terminal</h3>
      <p class="bento-card-desc">Preparas cambios, confirmas, abres un pull request y ves el estado de tus revisiones desde la misma pantalla. Para quienes prefieren ver, no teclear.</p>
    </div>

    <div class="bento-card bento-card--tall glass-card reveal reveal-delay-3">
      <span class="bento-card-label">MCP</span>
      <h3>Catálogo de herramientas</h3>
      <p class="bento-card-desc">Un catálogo visual de servidores MCP: buscas, instalas y listo. Sin archivos de configuración ni comandos raros. Le das permiso una vez y ya.</p>
      <div class="bento-swatches" aria-hidden="true">
        <span class="bento-swatch" style="background: var(--accent);"></span>
        <span class="bento-swatch" style="background: var(--coffee-400);"></span>
        <span class="bento-swatch" style="background: var(--coffee-600);"></span>
        <span class="bento-swatch" style="background: var(--coffee-800);"></span>
        <span class="bento-swatch" style="background: var(--bg-card);"></span>
        <span class="bento-swatch" style="background: var(--accent-soft);"></span>
      </div>
    </div>

    <div class="bento-card glass-card reveal reveal-delay-2">
      <div class="bento-card-icon" aria-hidden="true">
        <svg width="18" height="18" viewBox="0 0 20 20" fill="none">
          <rect x="2" y="3" width="16" height="12" rx="2" stroke="currentColor" stroke-width="1.5"/>
          <path d="M6 19h8M10 15v4" stroke="currentColor" stroke-width="1.5"/>
        </svg>
      </div>
      <h3>En tu escritorio, en el navegador y en el móvil</h3>
      <p class="bento-card-desc">Como app instalada, como web app o en el teléfono. Abres donde estés y encuentras exactamente lo que dejaste pendiente.</p>
    </div>

    <div class="bento-card glass-card reveal reveal-delay-3">
      <div class="bento-card-icon" aria-hidden="true">
        <svg width="18" height="18" viewBox="0 0 20 20" fill="none">
          <path d="M4 14l6-6-6-6M12 16h6" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
        </svg>
      </div>
      <h3>Se conecta con lo que ya usas</h3>
      <p class="bento-card-desc">Gmail, Slack, Google Drive, Notion y más de mil servicios más. Le das acceso con tu cuenta y Glenker empieza a mover tus cosas por ti.</p>
    </div>

    <div class="bento-card bento-card--accent reveal reveal-delay-4">
      <span class="bento-card-label">Skills</span>
      <h3>Automatizaciones que guardas</h3>
      <p class="bento-card-desc">Crea una rutina, guárdala y úsala cuando quieras: revisar correos, pasar notas a Notion, resumir lo del día. Las compartes o las dejas solo para ti.</p>
      <div class="bento-card-icon" style="margin-top: auto;" aria-hidden="true">
        <svg width="18" height="18" viewBox="0 0 20 20" fill="none">
          <rect x="7" y="2" width="6" height="10" rx="3" stroke="currentColor" stroke-width="1.5"/>
          <path d="M4 10a6 6 0 0012 0" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
          <path d="M10 16v3" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
        </svg>
      </div>
    </div>

    <div class="bento-card glass-card reveal reveal-delay-3">
      <div class="bento-card-icon" aria-hidden="true">
        <svg width="18" height="18" viewBox="0 0 20 20" fill="none">
          <circle cx="10" cy="10" r="8" stroke="currentColor" stroke-width="1.5"/>
          <path d="M10 6v4l3 3" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
        </svg>
      </div>
      <h3>Pasa de un lado a otro sin perder el hilo</h3>
      <p class="bento-card-desc">Empiezas en la computadora, sigues en la tablet y terminas desde el móvil. Si necesitas abrirlo en otro lado, un código QR te conecta al instante.</p>
    </div>

    <div class="bento-card glass-card reveal reveal-delay-4">
      <div class="bento-card-icon" aria-hidden="true">
        <svg width="18" height="18" viewBox="0 0 20 20" fill="none">
          <path d="M12 2l4 4-4 4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
          <path d="M2 10h10" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
          <path d="M2 14h14" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
          <path d="M2 18h12" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/>
        </svg>
      </div>
      <h3>Háblale, que él responde</h3>
      <p class="bento-card-desc">Dictas y Glenker te contesta en voz alta. Ideal cuando tienes las manos ocupadas o simplemente no te apetece escribir.</p>
    </div>
  </div>
</section>

<style is:global>
.bento {
  position: relative;
  z-index: 1;
  padding: 5rem 2rem;
}

.bento-grid {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  grid-auto-rows: 1fr;
  gap: 1rem;
}

.bento-card {
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: var(--radius-md);
  padding: 2rem;
  transition: border-color var(--transition) var(--ease-out),
    transform var(--transition) var(--ease-out),
    box-shadow var(--transition) var(--ease-out);
  display: flex;
  flex-direction: column;
  position: relative;
  overflow: hidden;
}

.bento-card:hover {
  border-color: var(--accent);
  transform: translateY(-2px);
  box-shadow: var(--shadow-md);
}

.bento-card--large {
  grid-column: span 2;
  grid-row: span 2;
}

.bento-card--tall {
  grid-column: span 1;
  grid-row: span 2;
}

.bento-card--accent {
  background: var(--accent-text);
  color: var(--text-white);
  border-color: var(--accent-text);
}

.bento-card--accent:hover {
  border-color: var(--accent-active);
  background: var(--accent-active);
  box-shadow: 0 12px 30px var(--accent-glow);
}

.bento-card--accent .bento-card-desc {
  color: rgba(255, 255, 255, 0.85);
}

.bento-card-label {
  font-family: var(--font-mono);
  font-size: 0.6rem;
  text-transform: uppercase;
  letter-spacing: 0.15em;
  color: var(--accent-active);
  margin-bottom: 0.5rem;
}

.bento-card--accent .bento-card-label {
  color: rgba(255, 255, 255, 0.7);
}

.bento-card-icon {
  width: 36px;
  height: 36px;
  display: flex;
  align-items: center;
  justify-content: center;
  background: var(--accent-soft);
  border-radius: 0.625rem;
  color: var(--accent-active);
  margin-bottom: 1rem;
  flex-shrink: 0;
}

.bento-card--accent .bento-card-icon {
  background: rgba(0, 0, 0, 0.12);
  color: var(--text-white);
}

.bento-card h3 {
  font-family: var(--font-heading);
  font-weight: 600;
  font-size: 1.1rem;
  letter-spacing: -0.03em;
  margin-bottom: 0.5rem;
  line-height: 1.3;
  color: var(--text-primary);
}

.bento-card--accent h3 {
  color: var(--text-white);
}

.bento-card-desc {
  font-size: 0.85rem;
  color: var(--text-secondary);
  line-height: 1.6;
  flex: 1;
}

.bento-chart {
  display: flex;
  align-items: flex-end;
  gap: 0.5rem;
  margin-top: auto;
  padding-top: 1.5rem;
  height: 120px;
}

.bento-bar {
  flex: 1;
  border-radius: 0.375rem 0.375rem 0 0;
  background: linear-gradient(to top, var(--accent-soft), var(--accent));
  min-height: 20px;
  transition: min-height 0.5s var(--ease-out);
}

.bento-card:hover .bento-bar {
  background: linear-gradient(to top, var(--accent), var(--accent-active));
}

.bento-swatches {
  display: flex;
  flex-wrap: wrap;
  gap: 0.5rem;
  margin-top: auto;
  padding-top: 1.5rem;
}

.bento-swatch {
  width: 36px;
  height: 36px;
  border-radius: 0.5rem;
  border: 1px solid var(--border);
}

@media (max-width: 1100px) {
  .bento-grid {
    grid-template-columns: repeat(3, 1fr);
  }

  .bento-card--large {
    grid-column: span 3;
  }

  .bento-card--tall {
    grid-row: span 1;
    grid-column: span 1;
  }
}

@media (max-width: 900px) {
  .bento-grid {
    grid-template-columns: repeat(2, 1fr);
  }

  .bento-card--large {
    grid-column: span 2;
    grid-row: span 1;
  }

  .bento-card--tall {
    grid-column: span 2;
    grid-row: span 1;
  }
}

@media (max-width: 600px) {
  .bento-grid {
    grid-template-columns: 1fr;
  }

  .bento-card--large,
  .bento-card--tall {
    grid-column: span 1;
  }
}
</style>
