<section class="cta-section">
  <h2>¿Le das una <span class="gradient-text">oportunidad</span>?</h2>
  <p>Descárgalo ahora y pruébalo con lo primero que tengas a mano. No cuesta nada y tardas menos de un minuto.</p>
  <a href="/#download" class="btn btn--primary btn--large">
    Descargar Ahora
    <svg width="16" height="16" viewBox="0 0 16 16" fill="none" aria-hidden="true">
      <path d="M8 3v10M4 9l4 4 4-4" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" />
    </svg>
  </a>
</section>

<style is:global>
.cta-section {
  position: relative;
  z-index: 1;
  padding: 3rem 2rem 5rem;
  text-align: center;
}

.cta-section h2 {
  font-family: var(--font-heading);
  font-weight: 700;
  font-size: clamp(2rem, 4vw, 3rem);
  letter-spacing: -0.06em;
  line-height: 1.1;
  color: var(--text-primary);
  margin-bottom: 0.75rem;
}

.cta-section p {
  color: var(--text-secondary);
  font-size: 1.05rem;
  margin-bottom: 2rem;
}
</style>
