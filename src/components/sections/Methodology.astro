<section class="methodology">
  <div class="methodology-grid">
    <div>
      <span class="section-label" style="color: rgba(0,0,0,0.4);">// Filosofía</span>
      <h2 class="section-title" style="font-size: clamp(2rem, 4vw, 2.75rem); color: #000; margin-bottom: 2.5rem;">
        IA para<br />
        <span style="color: rgba(0,0,0,0.5);">todos.</span>
      </h2>

      <div class="methodology-list">
        <div class="methodology-item">
          <div class="methodology-number">01</div>
          <div>
            <h3>Catálogo MCP</h3>
            <p>Navega e instala herramientas de IA con un clic. Como los conectores de Claude, pero en un catálogo visual con búsqueda y filtros.</p>
          </div>
        </div>
        <div class="methodology-item">
          <div class="methodology-number">02</div>
          <div>
            <h3>1000+ Integraciones</h3>
            <p>Conecta Gmail, Slack, Google Drive, Notion y más vía Composio. Tu asistente actúa sobre tus servicios favoritos.</p>
          </div>
        </div>
        <div class="methodology-item">
          <div class="methodology-number">03</div>
          <div>
            <h3>Multi-dispositivo</h3>
            <p>Desktop, web, PWA y móvil. Túneles Cloudflare con QR. La misma sesión en todas partes.</p>
          </div>
        </div>
      </div>
    </div>

    <div class="methodology-right">
      <div class="methodology-portrait" aria-label="User portrait">
        <svg width="80" height="80" viewBox="0 0 24 24" fill="rgba(0,0,0,0.3)">
          <path d="M12 12c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm0 2c-2.67 0-8 1.34-8 4v2h16v-2c0-2.66-5.33-4-8-4z"/>
        </svg>
      </div>
      <div class="methodology-testimonial">
        <p>"Glenker cambió mi forma de trabajar con IA. El catálogo MCP y las integraciones son increíbles."</p>
        <cite>— Comunidad Glenker</cite>
      </div>
    </div>
  </div>
</section>

<style is:global>
.methodology {
  background: var(--bg-contrast);
  color: #000000;
  border-radius: 4rem 4rem 0 0;
  position: relative;
  z-index: 1;
  padding: 5rem 2rem;
  margin-top: 2rem;
}

.methodology-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 4rem;
  align-items: center;
}

.methodology-list {
  display: flex;
  flex-direction: column;
  gap: 2.5rem;
}

.methodology-item {
  display: flex;
  gap: 1.5rem;
  align-items: flex-start;
}

.methodology-number {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 48px;
  height: 48px;
  border-radius: 50%;
  border: 1.5px solid rgba(0, 0, 0, 0.1);
  font-family: var(--font-mono);
  font-size: 0.85rem;
  font-weight: 500;
  flex-shrink: 0;
}

.methodology-item h3 {
  font-family: var(--font-heading);
  font-weight: 600;
  font-size: 1.2rem;
  letter-spacing: -0.03em;
  margin-bottom: 0.375rem;
}

.methodology-item p {
  font-size: 0.9rem;
  color: rgba(0, 0, 0, 0.6);
  line-height: 1.6;
}

.methodology-right {
  position: relative;
  display: flex;
  justify-content: center;
}

.methodology-portrait {
  width: 280px;
  height: 280px;
  border-radius: 50%;
  background: linear-gradient(135deg, #d4d4d4, #a3a3a3);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 5rem;
  filter: grayscale(1);
  position: relative;
}

.methodology-testimonial {
  position: absolute;
  bottom: -10px;
  left: -20px;
  right: -20px;
  padding: 1.25rem 1.5rem;
  background: rgba(255, 255, 255, 0.9);
  backdrop-filter: blur(16px);
  -webkit-backdrop-filter: blur(16px);
  border: 1px solid rgba(255, 255, 255, 0.3);
  border-radius: var(--radius-sm);
  box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
}

.methodology-testimonial p {
  font-size: 0.85rem;
  line-height: 1.6;
  color: rgba(0, 0, 0, 0.7);
  margin-bottom: 0.5rem;
}

.methodology-testimonial cite {
  font-style: normal;
  font-family: var(--font-mono);
  font-size: 0.7rem;
  color: rgba(0, 0, 0, 0.4);
  text-transform: uppercase;
  letter-spacing: 0.1em;
}
</style>