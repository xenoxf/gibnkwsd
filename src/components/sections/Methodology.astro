<section class="methodology">
  <div class="methodology-grid">
    <div>
      <span class="section-label">// Nuestra idea</span>
      <h2 class="section-title" style="font-size: clamp(2rem, 4vw, 2.75rem); color: var(--text-primary); margin-bottom: 2.5rem;">
        La IA no es<br />
        <span style="color: var(--text-secondary);">para unos pocos.</span>
      </h2>

      <div class="methodology-list">
        <div class="methodology-item">
          <div class="methodology-number">01</div>
          <div>
            <h3>Catálogo MCP</h3>
            <p>Navegas e instalas herramientas de IA con un clic. Igual que los conectores de Claude, pero pensado para verlo y entenderlo, no para adivinar comandos.</p>
          </div>
        </div>
        <div class="methodology-item">
          <div class="methodology-number">02</div>
          <div>
            <h3>1000+ integraciones</h3>
            <p>Conectas Gmail, Slack, Google Drive, Notion y un montón más. Glenker trabaja sobre lo que ya usas, no te pide que te cambies de app.</p>
          </div>
        </div>
        <div class="methodology-item">
          <div class="methodology-number">03</div>
          <div>
            <h3>Multi-dispositivo</h3>
            <p>Escritorio, web y móvil. Empiezas en uno y sigues en otro sin copiar nada: es la misma conversación, donde sea.</p>
          </div>
        </div>
      </div>
    </div>

    <div class="methodology-right">
      <div class="methodology-portrait" aria-label="Comunidad Glenker">
        <span class="methodology-monogram">G</span>
      </div>
      <figure class="methodology-testimonial">
        <blockquote>"Al principio no le tenía fe, la verdad. Luego dejé que me organizara el correo una mañana y no volví atrás. Se siente como tener a alguien que se adelanta."</blockquote>
        <cite>— Comunidad Glenker</cite>
      </figure>
    </div>
  </div>
</section>

<style is:global>
.methodology {
  background: var(--bg-contrast);
  color: var(--text-primary);
  border-radius: 4rem 4rem 0 0;
  position: relative;
  z-index: 1;
  padding: 5rem 2rem;
  margin-top: 2rem;
}

.methodology-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 4rem;
  align-items: center;
  max-width: 1100px;
  margin: 0 auto;
}

.methodology-list {
  display: flex;
  flex-direction: column;
  gap: 2.5rem;
}

.methodology-item {
  display: flex;
  gap: 1.5rem;
  align-items: flex-start;
}

.methodology-number {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 48px;
  height: 48px;
  border-radius: 50%;
  border: 1.5px solid var(--border);
  font-family: var(--font-mono);
  font-size: 0.85rem;
  font-weight: 500;
  color: var(--accent-active);
  flex-shrink: 0;
}

.methodology-item h3 {
  font-family: var(--font-heading);
  font-weight: 600;
  font-size: 1.2rem;
  letter-spacing: -0.03em;
  color: var(--text-primary);
  margin-bottom: 0.375rem;
}

.methodology-item p {
  font-size: 0.9rem;
  color: var(--text-secondary);
  line-height: 1.6;
}

.methodology-right {
  position: relative;
  display: flex;
  justify-content: center;
}

.methodology-portrait {
  width: 280px;
  height: 280px;
  border-radius: 50%;
  background: linear-gradient(135deg, var(--coffee-200), var(--coffee-400));
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: var(--shadow-md);
}

.methodology-monogram {
  font-family: var(--font-heading);
  font-size: 6rem;
  font-weight: 700;
  color: var(--text-white);
  letter-spacing: -0.04em;
}

.methodology-testimonial {
  position: absolute;
  bottom: -10px;
  left: -20px;
  right: -20px;
  margin: 0;
  padding: 1.25rem 1.5rem;
  background: var(--glass-bg);
  backdrop-filter: blur(16px);
  -webkit-backdrop-filter: blur(16px);
  border: 1px solid var(--border);
  border-radius: var(--radius-sm);
  box-shadow: var(--shadow-md);
}

.methodology-testimonial blockquote {
  margin: 0 0 0.5rem;
  font-size: 0.85rem;
  line-height: 1.6;
  color: var(--text-primary);
}

.methodology-testimonial cite {
  font-style: normal;
  font-family: var(--font-mono);
  font-size: 0.7rem;
  color: var(--text-muted);
  text-transform: uppercase;
  letter-spacing: 0.1em;
}

@media (max-width: 900px) {
  .methodology-grid {
    grid-template-columns: 1fr;
    gap: 3rem;
  }

  .methodology-right {
    order: -1;
  }

  .methodology {
    border-radius: 2rem 2rem 0 0;
  }

  .methodology-portrait {
    width: 200px;
    height: 200px;
  }

  .methodology-monogram {
    font-size: 4rem;
  }
}
</style>
