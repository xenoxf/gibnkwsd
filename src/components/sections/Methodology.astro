<section class="methodology">
  <div class="methodology-grid">
    <div>
      <span class="section-label">// Nuestra idea</span>
      <h2 class="section-title" style="font-size: clamp(2rem, 4vw, 2.75rem); color: var(--text-primary); margin-bottom: 2.5rem;">
        La IA no es<br />
        <span style="color: var(--text-secondary);">para unos pocos.</span>
      </h2>

      <div class="methodology-list">
        <div class="methodology-item">
          <div class="methodology-number">01</div>
          <div>
            <h3>Un fork con otra visión</h3>
            <p>Glenker nace de OpenChamber para llevar la experiencia de los agentes de código al usuario común, no solo a quien vive en la terminal. Mejoras de UX desde el primer día.</p>
          </div>
        </div>
        <div class="methodology-item">
          <div class="methodology-number">02</div>
          <div>
            <h3>Ver, no adivinar comandos</h3>
            <p>Catálogo de MCPs, skills reutilizables e integraciones pensadas para entenderse a la primera. Lo que ves es lo que obtienes, sin archivos de configuración.</p>
          </div>
        </div>
        <div class="methodology-item">
          <div class="methodology-number">03</div>
          <div>
            <h3>Multi-dispositivo</h3>
            <p>Escritorio, web y móvil. Empiezas en uno y sigues en otro sin copiar nada: es la misma sesión, donde sea.</p>
          </div>
        </div>
      </div>
    </div>

    <div class="methodology-right">
      <div class="methodology-portrait" aria-label="Glenker">
        <span class="methodology-monogram">G</span>
      </div>
    </div>
  </div>
</section>

<style is:global>
.methodology {
  background: var(--bg-contrast);
  color: var(--text-primary);
  border-radius: 4rem 4rem 0 0;
  position: relative;
  z-index: 1;
  padding: 5rem 2rem;
  margin-top: 2rem;
}

.methodology-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 4rem;
  align-items: center;
  max-width: 1100px;
  margin: 0 auto;
}

.methodology-list {
  display: flex;
  flex-direction: column;
  gap: 2.5rem;
}

.methodology-item {
  display: flex;
  gap: 1.5rem;
  align-items: flex-start;
}

.methodology-number {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 48px;
  height: 48px;
  border-radius: 50%;
  border: 1.5px solid var(--border);
  font-family: var(--font-mono);
  font-size: 0.85rem;
  font-weight: 500;
  color: var(--accent-active);
  flex-shrink: 0;
}

.methodology-item h3 {
  font-family: var(--font-heading);
  font-weight: 600;
  font-size: 1.2rem;
  letter-spacing: -0.03em;
  color: var(--text-primary);
  margin-bottom: 0.375rem;
}

.methodology-item p {
  font-size: 0.9rem;
  color: var(--text-secondary);
  line-height: 1.6;
}

.methodology-right {
  position: relative;
  display: flex;
  justify-content: center;
}

.methodology-portrait {
  width: 280px;
  height: 280px;
  border-radius: 50%;
  background: linear-gradient(135deg, var(--coffee-200), var(--coffee-400));
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: var(--shadow-md);
}

.methodology-monogram {
  font-family: var(--font-heading);
  font-size: 6rem;
  font-weight: 700;
  color: var(--text-white);
  letter-spacing: -0.04em;
}

@media (max-width: 900px) {
  .methodology-grid {
    grid-template-columns: 1fr;
    gap: 3rem;
  }

  .methodology-right {
    order: -1;
  }

  .methodology {
    border-radius: 2rem 2rem 0 0;
  }

  .methodology-portrait {
    width: 200px;
    height: 200px;
  }

  .methodology-monogram {
    font-size: 4rem;
  }
}
</style>
