<section class="section" id="changelog">
  <div class="section-header">
    <span class="section-label">// Updates</span>
    <h2 class="section-title" style="font-size: clamp(2rem, 4vw, 3rem);">What's <span class="gradient-text">New</span></h2>
    <p class="section-desc" style="margin-top: 0.75rem;">Latest changes and improvements in the most recent release.</p>
  </div>

  <div class="changelog-card reveal" id="changelog-section">
    <div class="changelog-header">
      <svg width="16" height="16" viewBox="0 0 20 20" fill="none">
        <path d="M4 4h12v12H4V4z" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" opacity="0.6"/>
        <path d="M7 10l2 2 4-4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" opacity="0.8"/>
      </svg>
      Changelog
    </div>
    <div id="changelog-content" class="changelog-content">
      <div class="skeleton" style="width: 30%"></div>
      <div class="skeleton" style="width: 50%"></div>
      <div class="skeleton" style="width: 90%"></div>
      <div class="skeleton" style="width: 70%"></div>
    </div>
    <div class="changelog-more">
      <a href="https://github.com/xenoxf/openjunior/releases" target="_blank" rel="noopener">View all releases →</a>
    </div>
  </div>
</section>

<style is:global>
.changelog-card {
  max-width: 560px;
  margin: 2rem auto 0;
  background: rgba(255, 255, 255, 0.03);
  backdrop-filter: blur(16px);
  -webkit-backdrop-filter: blur(16px);
  border: 1px solid rgba(255, 255, 255, 0.1);
  border-radius: var(--radius-sm);
  overflow: hidden;
}

.changelog-header {
  display: flex;
  align-items: center;
  gap: 0.5rem;
  padding: 1rem 1.5rem;
  border-bottom: 1px solid rgba(255, 255, 255, 0.06);
  font-size: 0.85rem;
  font-weight: 600;
  color: var(--text-secondary);
  font-family: var(--font-mono);
  text-transform: uppercase;
  letter-spacing: 0.1em;
}

.changelog-content {
  padding: 1.5rem;
  display: flex;
  flex-direction: column;
  gap: 0.75rem;
}

.changelog-item {
  display: flex;
  gap: 0.75rem;
  align-items: flex-start;
}

.changelog-bullet {
  width: 6px;
  height: 6px;
  border-radius: 50%;
  background: var(--accent);
  margin-top: 0.5rem;
  flex-shrink: 0;
}

.changelog-text {
  font-size: 0.85rem;
  color: var(--text-secondary);
  line-height: 1.6;
}

.changelog-more {
  padding: 1rem 1.5rem;
  border-top: 1px solid rgba(255, 255, 255, 0.06);
  text-align: center;
}

.changelog-more a {
  color: var(--accent);
  font-size: 0.85rem;
  font-weight: 500;
  transition: opacity 0.2s;
}

.changelog-more a:hover {
  opacity: 0.8;
}
</style>
