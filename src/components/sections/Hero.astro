<section class="hero">
  <div class="hero-grid">
    <div class="hero-left">
      <span class="ai-label">
        <span class="status-dot"></span>
        AI Code Agent
      </span>
      <h1 class="hero-title">
        Your AI<br />
        <span class="gradient-text">Code Agent</span>
      </h1>
      <p class="hero-desc">
        A free, open-source AI code assistant that helps you write, refactor, and understand code.
        Powered by OpenCode. Runs on your machine — no data leaves your computer.
      </p>
      <div class="hero-actions">
        <a href="#download" class="neon-btn">
          Download Free
          <svg width="16" height="16" viewBox="0 0 16 16" fill="none">
            <path d="M8 3v10M4 9l4 4 4-4" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" />
          </svg>
        </a>
        <a href="https://github.com/xenoxf/openjunior" target="_blank" rel="noopener" class="neon-btn neon-btn--outline">
          <svg width="16" height="16" viewBox="0 0 16 16" fill="currentColor">
            <path d="M8 0C3.58 0 0 3.58 0 8c0 3.54 2.29 6.53 5.47 7.59.4.07.55-.17.55-.38 0-.19-.01-.82-.01-1.49-2.01.37-2.53-.49-2.69-.94-.09-.23-.48-.94-.82-1.13-.28-.15-.68-.52-.01-.53.63-.01 1.08.58 1.23.82.72 1.21 1.87.87 2.33.66.07-.52.28-.87.51-1.07-1.78-.2-3.64-.89-3.64-3.95 0-.87.31-1.59.82-2.15-.08-.2-.36-1.02.08-2.12 0 0 .67-.21 2.2.82.64-.18 1.32-.27 2-.27.68 0 1.36.09 2 .27 1.53-1.04 2.2-.82 2.2-.82.44 1.1.16 1.92.08 2.12.51.56.82 1.27.82 2.15 0 3.07-1.87 3.75-3.65 3.95.29.25.54.73.54 1.48 0 1.07-.01 1.93-.01 2.2 0 .21.15.46.55.38A8.013 8.013 0 0016 8c0-4.42-3.58-8-8-8z" />
          </svg>
          Source Code
        </a>
      </div>
    </div>

    <div class="hero-right">
      <div class="mockup">
        <div class="mockup-main">
          <div class="mockup-header">
            <div class="mockup-dots">
              <span class="mockup-dot"></span>
              <span class="mockup-dot"></span>
              <span class="mockup-dot"></span>
            </div>
            <span class="mockup-title">openjunior — bash</span>
          </div>
          <div class="mockup-body">
            <div class="mockup-msg">
              <strong>OpenJunior</strong> ready · 4 agents online
            </div>
            <div class="mockup-code">
              <span>$</span> openjunior --ui-password<br />
              <span>></span> Server running on port 3000
            </div>
            <div class="mockup-msg mockup-msg--user">
              Review the diff and create a PR
            </div>
            <div class="mockup-code">
              <span>></span> Branch: feature/new-ui<br />
              <span>></span> Changes: 12 files · +245 −89
            </div>
          </div>
        </div>

        <div class="mockup-card--floating top-right">
          <div class="mockup-float-label">
            <span class="status-dot"></span>
            Agents Active
          </div>
          <div class="mockup-float-value">4</div>
        </div>

        <div class="mockup-card--floating bottom-left">
          <div class="mockup-float-label">
            <span class="status-dot"></span>
            Sessions
          </div>
          <div class="mockup-float-value">12</div>
        </div>

        <div class="ai-cursor-label">AI Cursor</div>
      </div>
    </div>
  </div>
</section>

<style is:global>
.hero {
  position: relative;
  z-index: 1;
  padding: 2rem 2rem 4rem;
}

.hero-grid {
  display: grid;
  grid-template-columns: 7fr 5fr;
  gap: 3rem;
  align-items: center;
}

.hero-left {
  position: relative;
}

.ai-label {
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
  padding: 0.375rem 0.75rem;
  font-family: var(--font-mono);
  font-size: 0.65rem;
  text-transform: uppercase;
  letter-spacing: 0.2em;
  color: var(--accent);
  background: rgba(204, 255, 0, 0.08);
  border: 1px solid rgba(204, 255, 0, 0.15);
  border-radius: 0.375rem;
  margin-bottom: 1.25rem;
}

.hero-title {
  font-family: var(--font-heading);
  font-size: clamp(3rem, 7.5vw, 7.5rem);
  font-weight: 700;
  letter-spacing: -0.06em;
  line-height: 0.85;
  margin-bottom: 1.5rem;
}

.hero-desc {
  font-size: 1.05rem;
  color: var(--text-secondary);
  max-width: 480px;
  line-height: 1.7;
  margin-bottom: 2rem;
}

.hero-actions {
  display: flex;
  align-items: center;
  gap: 1rem;
  flex-wrap: wrap;
}

.hero-right {
  position: relative;
  min-height: 400px;
}

.mockup {
  position: relative;
  width: 100%;
  height: 480px;
}

.mockup-main {
  position: absolute;
  inset: 0;
  background: rgba(255, 255, 255, 0.03);
  backdrop-filter: blur(16px);
  -webkit-backdrop-filter: blur(16px);
  border: 1px solid rgba(255, 255, 255, 0.1);
  border-radius: var(--radius-sm);
  overflow: hidden;
  animation: float 6s ease-in-out infinite;
}

.mockup-header {
  display: flex;
  align-items: center;
  gap: 0.5rem;
  padding: 1rem 1.25rem;
  border-bottom: 1px solid rgba(255, 255, 255, 0.06);
}

.mockup-dots {
  display: flex;
  gap: 6px;
}

.mockup-dot {
  width: 10px;
  height: 10px;
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.15);
}

.mockup-dot:nth-child(1) { background: #ef4444; }
.mockup-dot:nth-child(2) { background: #eab308; }
.mockup-dot:nth-child(3) { background: #22c55e; }

.mockup-title {
  font-family: var(--font-mono);
  font-size: 0.7rem;
  color: var(--text-muted);
  margin-left: auto;
  text-transform: uppercase;
  letter-spacing: 0.1em;
}

.mockup-body {
  padding: 1.25rem;
  display: flex;
  flex-direction: column;
  gap: 0.75rem;
}

.mockup-msg {
  padding: 0.75rem 1rem;
  background: rgba(204, 255, 0, 0.08);
  border: 1px solid rgba(204, 255, 0, 0.12);
  border-radius: 0.75rem;
  font-size: 0.8rem;
  color: var(--text-secondary);
  line-height: 1.5;
  max-width: 85%;
}

.mockup-msg--user {
  align-self: flex-end;
  background: rgba(255, 255, 255, 0.05);
  border-color: rgba(255, 255, 255, 0.08);
}

.mockup-msg strong {
  color: var(--accent);
  font-weight: 500;
}

.mockup-code {
  padding: 0.75rem 1rem;
  background: rgba(0, 0, 0, 0.3);
  border-radius: 0.75rem;
  font-family: var(--font-mono);
  font-size: 0.75rem;
  color: var(--accent);
  line-height: 1.6;
  max-width: 90%;
}

.mockup-code span {
  color: var(--text-muted);
}

.mockup-card--floating {
  position: absolute;
  width: 180px;
  padding: 1rem;
  background: rgba(255, 255, 255, 0.05);
  backdrop-filter: blur(16px);
  -webkit-backdrop-filter: blur(16px);
  border: 1px solid rgba(255, 255, 255, 0.1);
  border-radius: 1rem;
  box-shadow: 0 8px 32px rgba(0, 0, 0, 0.4);
}

.mockup-card--floating.top-right {
  top: -10px;
  right: -20px;
  animation: float 6s ease-in-out infinite;
  animation-delay: -2s;
}

.mockup-card--floating.bottom-left {
  bottom: 20px;
  left: -30px;
  animation: float-slow 6s ease-in-out infinite;
  animation-delay: -4s;
}

.mockup-float-label {
  display: flex;
  align-items: center;
  gap: 0.375rem;
  font-family: var(--font-mono);
  font-size: 0.6rem;
  text-transform: uppercase;
  letter-spacing: 0.15em;
}

.mockup-float-label .status-dot {
  width: 5px;
  height: 5px;
}

.mockup-float-value {
  font-family: var(--font-heading);
  font-size: 1.1rem;
  font-weight: 700;
  letter-spacing: -0.03em;
  margin-top: 0.25rem;
}

.ai-cursor-label {
  position: absolute;
  bottom: -10px;
  right: 20px;
  padding: 0.5rem 1rem;
  background: var(--accent);
  color: #000000;
  font-family: var(--font-mono);
  font-size: 0.65rem;
  text-transform: uppercase;
  letter-spacing: 0.15em;
  font-weight: 600;
  border-radius: 0.5rem;
  box-shadow: 0 4px 20px rgba(204, 255, 0, 0.4);
  animation: float 4s ease-in-out infinite;
  animation-delay: -1s;
  z-index: 2;
}
</style>
