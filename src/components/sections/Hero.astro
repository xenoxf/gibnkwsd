<section class="hero">
    <div class="hero-grid">
        <div class="hero-left">
            <h1 class="hero-title">Glenker</h1>
            <p class="hero-desc">
                Glenker es un asistente de inteligencia artificial para
                cualquier persona. Conecta con 1000+ servicios (Gmail, Slack,
                Google Drive, Notion), instala herramientas con un clic desde el
                catálogo MCP, y automatiza tareas con Skills reutilizables.
                Desktop, web y móvil.
            </p>
            <div class="hero-actions">
                <a href="#download" class="btn">
                    Descargar Gratis
                    <svg width="16" height="16" viewBox="0 0 16 16" fill="none">
                        <path
                            d="M8 3v10M4 9l4 4 4-4"
                            stroke="currentColor"
                            stroke-width="1.8"
                            stroke-linecap="round"
                            stroke-linejoin="round"></path>
                    </svg>
                </a>
            </div>
        </div>
            </div><div class="container-img" >
              <img src="https://openchamber.dev/desktop-chat-light.png" alt="">
        </div>

</section>

<style is:global>
    .hero {
        position: relative;
        z-index: 1;
        padding: 2rem 2rem 4rem;
display: flex;
      justify-content: center;
      align-items: center;
      flex-direction: column;
      gap: 1rem;
    }

    .hero-grid {
      display: flex;
      justify-content: center;
      align-items: center;
      flex-direction: column;
      align-items: center;
    }

    .hero-left {
      position: relative;
      
    }

    .container-img {
      width: 90%;
      border: 2px solid var(--accent);

    }

    .ai-label {
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        padding: 0.375rem 0.75rem;
        font-family: var(--font-mono);
        font-size: 0.65rem;
        text-transform: uppercase;
        letter-spacing: 0.2em;
        color: var(--accent);
        background: rgba(204, 255, 0, 0.08);
        border: 1px solid rgba(204, 255, 0, 0.15);
        border-radius: 0.375rem;
        margin-bottom: 1.25rem;
    }
.btn {
  background: var(--accent);
  display: flex;
  flex-direction: row;
  align-items: center;
  justify-content: center;
      color: black;
      padding: 1rem;
    }
    .hero-title {
        font-family: var(--font-heading);
        font-size: clamp(3rem, 7.5vw, 7.5rem);
        font-weight: 700;
        letter-spacing: -0.06em;
        line-height: 0.85;
        margin-bottom: 1.5rem;
    }

    .hero-desc {
        font-size: 1.05rem;
        color: var(--text-secondary);
        max-width: 480px;
        line-height: 1.7;
        margin-bottom: 2rem;
    }

    .hero-actions {
        display: flex;
        align-items: center;
        gap: 1rem;
        flex-wrap: wrap;
    }

    .hero-right {
        position: relative;
        min-height: 400px;
    }

    .mockup {
        position: relative;
        width: 100%;
        height: 480px;
    }

    .mockup-main {
        position: absolute;
        inset: 0;
        background: rgba(255, 255, 255, 0.03);
        backdrop-filter: blur(16px);
        -webkit-backdrop-filter: blur(16px);
        border: 1px solid rgba(255, 255, 255, 0.1);
        border-radius: var(--radius-sm);
        overflow: hidden;
        animation: float 6s ease-in-out infinite;
    }

    .mockup-header {
        display: flex;
        align-items: center;
        gap: 0.5rem;
        padding: 1rem 1.25rem;
        border-bottom: 1px solid rgba(255, 255, 255, 0.06);
    }

    .mockup-dots {
        display: flex;
        gap: 6px;
    }

    .mockup-dot {
        width: 10px;
        height: 10px;
        border-radius: 50%;
        background: rgba(255, 255, 255, 0.15);
    }

    .mockup-dot:nth-child(1) {
        background: #ef4444;
    }
    .mockup-dot:nth-child(2) {
        background: #eab308;
    }
    .mockup-dot:nth-child(3) {
        background: #22c55e;
    }

    .mockup-title {
        font-family: var(--font-mono);
        font-size: 0.7rem;
        color: var(--text-muted);
        margin-left: auto;
        text-transform: uppercase;
        letter-spacing: 0.1em;
    }

    .mockup-body {
        padding: 1.25rem;
        display: flex;
        flex-direction: column;
        gap: 0.75rem;
    }

    .mockup-msg {
        padding: 0.75rem 1rem;
        background: rgba(204, 255, 0, 0.08);
        border: 1px solid rgba(204, 255, 0, 0.12);
        border-radius: 0.75rem;
        font-size: 0.8rem;
        color: var(--text-secondary);
        line-height: 1.5;
        max-width: 85%;
    }

    .mockup-msg--user {
        align-self: flex-end;
        background: rgba(255, 255, 255, 0.05);
        border-color: rgba(255, 255, 255, 0.08);
    }

    .mockup-msg strong {
        color: var(--accent);
        font-weight: 500;
    }

    .mockup-code {
        padding: 0.75rem 1rem;
        background: rgba(0, 0, 0, 0.3);
        border-radius: 0.75rem;
        font-family: var(--font-mono);
        font-size: 0.75rem;
        color: var(--accent);
        line-height: 1.6;
        max-width: 90%;
    }

    .mockup-code span {
        color: var(--text-muted);
    }

    .mockup-card--floating {
        position: absolute;
        width: 180px;
        padding: 1rem;
        background: rgba(255, 255, 255, 0.05);
        backdrop-filter: blur(16px);
        -webkit-backdrop-filter: blur(16px);
        border: 1px solid rgba(255, 255, 255, 0.1);
        border-radius: 1rem;
        box-shadow: 0 8px 32px rgba(0, 0, 0, 0.4);
    }

    .mockup-card--floating.top-right {
        top: -10px;
        right: -20px;
        animation: float 6s ease-in-out infinite;
        animation-delay: -2s;
    }

    .mockup-card--floating.bottom-left {
        bottom: 20px;
        left: -30px;
        animation: float-slow 6s ease-in-out infinite;
        animation-delay: -4s;
    }

    .mockup-float-label {
        display: flex;
        align-items: center;
        gap: 0.375rem;
        font-family: var(--font-mono);
        font-size: 0.6rem;
        text-transform: uppercase;
        letter-spacing: 0.15em;
    }

    .mockup-float-label .status-dot {
        width: 5px;
        height: 5px;
    }

    .mockup-float-value {
        font-family: var(--font-heading);
        font-size: 1.1rem;
        font-weight: 700;
        letter-spacing: -0.03em;
        margin-top: 0.25rem;
    }

    .ai-cursor-label {
        position: absolute;
        bottom: -10px;
        right: 20px;
        padding: 0.5rem 1rem;
        background: var(--accent);
        color: #000000;
        font-family: var(--font-mono);
        font-size: 0.65rem;
        text-transform: uppercase;
        letter-spacing: 0.15em;
        font-weight: 600;
        border-radius: 0.5rem;
        box-shadow: 0 4px 20px rgba(204, 255, 0, 0.4);
        animation: float 4s ease-in-out infinite;
        animation-delay: -1s;
        z-index: 2;
    }
</style>
