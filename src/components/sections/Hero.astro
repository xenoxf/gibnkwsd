<section class="hero">
  <div class="hero-inner">
    <h1 class="hero-title">Glenker</h1>
    <p class="hero-desc">
      Una interfaz visual y completa para tus agentes de código. Revisa diffs,
      gestiona agentes, corre servidores y mantén el panorama completo mientras
      la IA programa por ti. En el escritorio, en el navegador o en tu celular.
    </p>
    <div class="hero-actions">
      <a href="/#download" class="btn btn--primary btn--large">
        Descargar Gratis
        <svg width="16" height="16" viewBox="0 0 16 16" fill="none" aria-hidden="true">
          <path d="M8 3v10M4 9l4 4 4-4" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" />
        </svg>
      </a>
    </div>
    <p class="hero-note"> Gratis · Hecho para cualquiera</p>
  </div>

  <div class="hero-media">
    <img
      src="/screenshots/chat_example.png"
      alt="Glenker en escritorio — chat con el agente de código, diffs y herramientas"
      width="1600"
      height="900"
      loading="eager"
    />
  </div>
</section>

<style is:global>
.hero {
  position: relative;
  z-index: 1;
  padding: 3rem 2rem 4rem;
}

.hero-inner {
  max-width: 760px;
  margin: 0 auto;
  text-align: center;
  display: flex;
  flex-direction: column;
  align-items: center;
}

.hero-eyebrow {
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
  padding: 0.375rem 0.85rem;
  font-family: var(--font-mono);
  font-size: 0.7rem;
  text-transform: uppercase;
  letter-spacing: 0.18em;
  color: var(--accent-text);
  background: var(--accent-soft);
  border: 1px solid var(--border-light);
  border-radius: var(--radius-full);
  margin-bottom: 1.5rem;
}

.hero-title {
  font-family: var(--font-heading);
  font-size: clamp(4rem, 12vw, 9rem);
  font-weight: 700;
  letter-spacing: -0.06em;
  line-height: 0.9;
  color: var(--text-primary);
  margin-bottom: 1.5rem;
}

.hero-desc {
  font-size: 1.1rem;
  color: var(--text-secondary);
  max-width: 560px;
  line-height: 1.7;
  margin-bottom: 2rem;
}

.hero-actions {
  display: flex;
  align-items: center;
  gap: 0.75rem;
  flex-wrap: wrap;
  justify-content: center;
}

.hero-note {
  margin-top: 1.25rem;
  font-family: var(--font-mono);
  font-size: 0.75rem;
  color: var(--text-muted);
  letter-spacing: 0.04em;
}

.hero-media {
  margin: 3.5rem auto 0;
  max-width: 1200px;
  width: 100%;
  border: 1px solid var(--border);
  border-radius: var(--radius-lg);
  overflow: hidden;
  background: var(--bg-card);
  box-shadow: var(--shadow-lg);
  transition: box-shadow var(--transition) var(--ease-out);
}

.hero-media:hover {
  box-shadow: var(--shadow-xl);
}

.hero-media img {
  display: block;
  width: 100%;
  height: auto;
}

@media (max-width: 600px) {
  .hero {
    padding: 2rem 1.25rem 3rem;
  }

  .hero-actions {
    flex-direction: column;
    align-items: stretch;
  }

  .hero-actions .btn {
    justify-content: center;
  }

  .hero-media {
    margin-top: 2.5rem;
    border-radius: var(--radius-md);
  }
}
</style>
