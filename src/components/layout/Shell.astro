<div class="shell">
  <div class="shell-grid"></div>
  <div class="glow-sphere glow-sphere--accent"></div>
  <div class="glow-sphere glow-sphere--coffee"></div>
  <slot />
</div>

<style is:global>
.shell {
  position: relative;
  max-width: var(--shell-max);
  margin: 0 auto;
  background: var(--bg-shell);
  border-radius: var(--radius-lg);
  box-shadow: var(--shadow-lg);
  overflow: hidden;
  min-height: 100vh;
}

.shell-grid {
  position: absolute;
  inset: 0;
  pointer-events: none;
  z-index: 0;
  background-image: linear-gradient(var(--border-light) 1px, transparent 1px),
    linear-gradient(90deg, var(--border-light) 1px, transparent 1px);
  background-size: 64px 64px;
  mask-image: radial-gradient(ellipse at 50% 0%, black 20%, transparent 75%);
  -webkit-mask-image: radial-gradient(ellipse at 50% 0%, black 20%, transparent 75%);
}

.glow-sphere {
  position: absolute;
  pointer-events: none;
  border-radius: 50%;
  filter: blur(120px);
  z-index: 0;
  animation: float-y 18s ease-in-out infinite;
}

.glow-sphere--accent {
  width: 520px;
  height: 520px;
  background: radial-gradient(circle, var(--accent-glow) 0%, transparent 70%);
  top: -220px;
  right: -120px;
  animation-duration: 20s;
}

.glow-sphere--coffee {
  width: 420px;
  height: 420px;
  background: radial-gradient(circle, rgba(154, 109, 69, 0.12) 0%, transparent 70%);
  bottom: -140px;
  left: -120px;
  animation-duration: 24s;
  animation-delay: -7s;
}
</style>
